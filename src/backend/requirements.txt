# setlist-maker 백엔드 (파일럿) 의존성
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.12.2
httpx==0.27.2
pykakasi==2.3.0  # 곡 제목 로마자/한글 음차 검색 보조(순수 파이썬, 무거운 의존성 없음)
hanja==0.2.6  # 한자 → 한글 음독(한국 한자음) 변환

# 개발/테스트
pytest==9.1.0
