# audio assets (copyright — never commit; iframe 재생만 사용, PRD §7)
*.wav
*.mp3
*.flac
*.m4a
*.aac
*.ogg
*.opus
audio_cache/
**/audio_cache/
**/audio/

# raw feature re-extraction dumps
*.npy
*.npz

# 외부 참고 논문(저작권·대용량 바이너리 — 로컬 유지, R&D가 로컬에서 참조)
docs/ref/**/*.pdf

# python
__pycache__/
*.pyc
venv/
.venv/

# env / secrets
.env
secret*.json
