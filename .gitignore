# audio assets (copyright — never commit; wav cache lives in the sibling
# bandori-song-sorter repo, not here, but keep the rule as a safety net)
*.wav
*.mp3
*.flac
*.m4a
*.aac
*.ogg
*.opus
audio_cache/
**/audio_cache/
**/audio/

# raw feature re-extraction dumps
*.npy
*.npz

# python
__pycache__/
*.pyc
venv/
.venv/

# env / secrets
.env
secret*.json
